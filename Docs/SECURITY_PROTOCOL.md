**Security Constraint:** All file system access (read, write, delete) performed by the model or the harness must pass through a dedicated, centralized **System/File I/O Manager Service**. This service must implement a strict, configurable Allow/Deny list system. The default policy must be to **DENY** all actions. This system must be designed first, before integrating any writing functionality, to prevent unauthorized access to the host OS or restricted directories.